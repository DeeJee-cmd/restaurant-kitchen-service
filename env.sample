# Django
SECRET_KEY=django-insecure-change-me
DJANGO_DEBUG=True
DJANGO_SETTINGS_MODULE=kitchen_service.settings.dev

# Comma-separated list of allowed hosts (used in production settings)
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (required only for production / PostgreSQL settings)
DB_NAME=kitchen_db
DB_USER=kitchen_user
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432
